{
  "name": "urlshortner",
  "version": "1.0.0",
  "description": "app to shorten the large url to short url",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "dev": "yarn nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chandru K",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "mongoose": "^8.2.1",
    "nanoid": "3.3.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
